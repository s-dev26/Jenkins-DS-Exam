apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "exam-jenkins.fullname" . }}-test-connection"
  labels:
    {{- include "exam-jenkins.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "exam-jenkins.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

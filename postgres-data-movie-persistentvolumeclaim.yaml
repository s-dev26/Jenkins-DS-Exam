apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: postgres-data-movie
  name: postgres-data-movie
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
